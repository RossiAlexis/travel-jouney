# mobile

> Expo + React Native mobile app — coming soon.

This app will be scaffolded with the latest Expo SDK using React Native and TypeScript.

It will share types from `@repo/types` and configs from `@repo/config`.

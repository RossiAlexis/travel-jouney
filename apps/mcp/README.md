# mcp

> MCP (Model Context Protocol) server — coming soon.

This app will be a minimal TypeScript Node.js MCP server that exposes travel data tools to AI assistants.

It will share types from `@repo/types` and configs from `@repo/config`.



Use **[Readme.md](./instruction/README.md)** to get the context for the app

Always use Context7 MCP when I need library/API documentation, code generation, setup or configuration steps without me having to explicitly ask.

# Skills

When working on tasks related to the following topics, consult and follow the guidelines in the corresponding skill files under `docs/skills/`:

- `package-manager` — package management (installing, adding, removing dependencies)